<%= form_for(@survey_answer) do |f| %>
  <% if @survey_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey_answer.errors.count, "error") %> prohibited this survey_answer from being saved:</h2>

      <ul>
      <% @survey_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :vote_id %><br>
    <%= f.number_field :vote_id %>
  </div>
  <div class="field">
    <%= f.label :question %><br>
    <%= f.text_field :question %>
  </div>
  <div class="field">
    <%= f.label :answer %><br>
    <%= f.text_field :answer %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
